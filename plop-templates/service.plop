import { Request } from 'express'

import { ServiceAPIResponse } from '../../types/service-response'
import { {{camelcase name}} } from '../../types/{{dashCase name}}'

const getAll = async (): Promise<ServiceAPIResponse<{{camelcase name}}[]>> => {
  /* fetch data here */
  return {
    statusCode: 200,
    body: [
      {
        title: '{{upperCase name}} title',
      }
    ]
  }
}

export { getAll }
